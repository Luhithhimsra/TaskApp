# Database Configuration
# Copy these values into DB_connection.php

DB_HOST=localhost
DB_NAME=task_management_db
DB_USER=root
DB_PASS=

# Do NOT commit real credentials to Git
# Add DB_connection.php to .gitignore if it contains real passwords
